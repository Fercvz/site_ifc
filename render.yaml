services:
  - type: web
    name: ifcz-backend
    runtime: docker
    region: oregon
    plan: free
    rootDir: backend
    envVars:
      - key: PORT
        value: 10000
      - key: OPENAI_API_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://site-ifc.vercel.app
